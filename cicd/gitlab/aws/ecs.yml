deploy_ecs:
  stage: deploy
  image: amazon/aws-cli:2.15.0
  script:
    - aws ecs update-service \
        --cluster $ECS_CLUSTER \
        --service $ECS_SERVICE \
        --force-new-deployment
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
